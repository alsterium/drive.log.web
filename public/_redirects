# Redirect all routes to index.html for client-side routing
# This ensures that direct navigation to any route works correctly
# with Next.js static export and client-side routing

# Handle SPA routing - redirect all non-file requests to index.html
/*    /index.html   200

# Handle specific routes if needed (currently not needed for this single-page app)
# /about    /index.html   200
# /contact  /index.html   200